"auto-indenting based on filename
filetype indent plugin on

"syntax highlighting
syntax on

"command-line completion
set wildmenu

"highlight searches
set hlsearch

"case insensitive search, unless pattern contains upper
set ignorecase
set smartcase

"copy indent from current line when starting a new line
set autoindent

"display line numbers
set number

"indent configuration
set shiftwidth=4    "auto indent 4 spaces
set softtabstop=4   "use 4 spaces as a soft-tab
set noexpandtab     "use SP(space) instead HT(tab)
